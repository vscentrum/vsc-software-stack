--- tools/iguidelib/setup.py.orig	2025-05-08 12:57:48.790022705 +0200
+++ tools/iguidelib/setup.py	2025-05-08 12:58:51.751711273 +0200
@@ -1,18 +1,10 @@
 from os import getenv, getcwd
-from subprocess import run, PIPE
 from setuptools import setup, find_packages
 
 def get_iguide_version(with_hash = False):
     iguide_version_path = getenv("IGUIDE_DIR", getcwd()) + "/.version"
     iguide_version = open(iguide_version_path, "r").readlines()[0].rstrip()
-    commit_hash = run(
-      ["git", "rev-parse", "--short", "HEAD"], stdout=PIPE
-      )
-    commit_str = commit_hash.stdout.decode('utf-8').rstrip()
-    if with_hash:
-        return iguide_version + "+" + commit_str
-    else:
-        return iguide_version
+    return iguide_version
 
 setup(
     name = "iguide",
--- tools/iguidelib/iguidelib/__init__.py.orig	2025-05-08 13:19:55.894433484 +0200
+++ tools/iguidelib/iguidelib/__init__.py	2025-05-08 13:19:24.907867324 +0200
@@ -77,21 +77,8 @@
         )
 
     iguide_version = open(iguide_version_path, "r").readlines()[0].rstrip()
-
-    wd = getcwd()
-    chdir(str(iguide_path))
-
-    commit_hash = run(
-      ["git", "rev-parse", "--short", "HEAD"], stdout=PIPE
-    )
-
-    chdir(wd)
-
-    commit_str = commit_hash.stdout.decode('utf-8').rstrip()
-
-    if with_hash:
-        return iguide_version + "+" + commit_str
-    else:
-        return iguide_version
-
-__version__ = get_iguide_version( with_hash = True )
+
+    return iguide_version
+__version__ = get_iguide_version( with_hash = False )