name = 'NVHPC'
version = '25.3'
versionsuffix = '-CUDA-%(cudaver)s'

homepage = 'https://developer.nvidia.com/hpc-sdk/'
description = """Complete toolchain based on NVIDIA HPC SDK. Includes C, C++ and FORTRAN
compilers (nvidia-compilers), an MPI implementation based on OpenMPI (NVHPCX)
and math libraries based on OpenBLAS and ScaLAPACK."""

toolchain = SYSTEM

# By downloading, you accept the HPC SDK Software License Agreement
# https://docs.nvidia.com/hpc-sdk/eula/index.html
local_tarball_tmpl = 'nvhpc_2025_%%(version_major)s%%(version_minor)s_Linux_%s_cuda_multi.tar.gz'
source_urls = ['https://developer.download.nvidia.com/hpc-sdk/%(version)s/']
sources = [local_tarball_tmpl % '%(arch)s']
checksums = [
    {
        local_tarball_tmpl % 'aarch64':
            'a2b86cf5141c0a9b0925999521693981451a8d2403367c36c46238163be6f2bb',
        local_tarball_tmpl % 'x86_64':
            'e2b2c911478a5db6a15d1fd258a8c4004dbfccf6f32f4132fe142a24fb7e6f8f',
    }
]

dependencies = [
    ('CUDA', '12.8.0'),
    ('nvidia-compilers', version, versionsuffix),
]

# NVHPC needs CUDA to work. This easyconfig uses external CUDA from EasyBuild.
# The default CUDA version used by NVHPC will be the version of loaded CUDA module

# The default Compute Capability used by NVHPC is the one defined in nvidia-compilers (if any)

moduleclass = 'toolchain'

# TODO
#   >> running command 'mpirun -n 8 /tmp/vsc45304/eb-c5ag0xph/tmp37cyz96r/mpi_test_hello_c' ...
#   >> result for command 'mpirun -n 8 /tmp/vsc45304/eb-c5ag0xph/tmp37cyz96r/mpi_test_hello_c': FAILED
# == ... (took 10 secs)
# == FAILED: Installation ended unsuccessfully: Sanity check failed: sanity check command mpirun -n 8 /tmp/vsc45304/eb-c5ag0xph/tmp37cyz96r/mpi_test_hello_c failed with exit code 1 (output:
# --------------------------------------------------------------------------
# There are not enough slots available in the system to satisfy the 8
# slots that were requested by the application:
#
#   /tmp/vsc45304/eb-c5ag0xph/tmp37cyz96r/mpi_test_hello_c
#
# Either request fewer slots for your application, or make more slots
# available for use.
#
# A "slot" is the Open MPI term for an allocatable unit where we can
# launch a process.  The number of slots available are defined by the
# environment in which Open MPI processes are run:
#
#   1. Hostfile, via "slots=N" clauses (N defaults to number of
#      processor cores if not provided)
#   2. The --host command line parameter, via a ":N" suffix on the
#      hostname (N defaults to 1 if not provided)
#   3. Resource manager (e.g., SLURM, PBS/Torque, LSF, etc.)
#   4. If none of a hostfile, the --host command line parameter, or an
#      RM is present, Open MPI defaults to the number of processor cores
#
# In all the above cases, if you want Open MPI to default to the number
# of hardware threads instead of the number of processor cores, use the
# --use-hwthread-cpus option.
#
# Alternatively, you can use the --oversubscribe option to ignore the
# number of available slots when deciding the number of processes to
# launch.
# --------------------------------------------------------------------------
# ) (took 5 mins 54 secs)
# == Results of the build can be found in the log file(s) /tmp/vsc45304/eb-c5ag0xph/easybuild-NVHPC-25.3-20250826.122724.bWrSQ.log
# == Summary:
#    * [FAILED]  NVHPC/25.3
#
# ERROR: Installation of NVHPC-25.3.eb failed: 'Sanity check failed: sanity check command mpirun -n 8 /tmp/vsc45304/eb-c5ag0xph/tmp37cyz96r/mpi_test_hello_c failed with exit code 1 (output: --------------------------------------------------------------------------\nThere are not enough slots available in the system to satisfy the 8\nslots that were requested by the application:\n\n  /tmp/vsc45304/eb-c5ag0xph/tmp37cyz96r/mpi_test_hello_c\n\nEither request fewer slots for your application, or make more slots\navailable for use.\n\nA "slot" is the Open MPI term for an allocatable unit where we can\nlaunch a process.  The number of slots available are defined by the\nenvironment in which Open MPI processes are run:\n\n  1. Hostfile, via "slots=N" clauses (N defaults to number of\n     processor cores if not provided)\n  2. The --host command line parameter, via a ":N" suffix on the\n     hostname (N defaults to 1 if not provided)\n  3. Resource manager (e.g., SLURM, PBS/Torque, LSF, etc.)\n  4. If none of a hostfile, the --host command line parameter, or an\n     RM is present, Open MPI defaults to the number of processor cores\n\nIn all the above cases, if you want Open MPI to default to the number\nof hardware threads instead of the number of processor cores, use the\n--use-hwthread-cpus option.\n\nAlternatively, you can use the --oversubscribe option to ignore the\nnumber of available slots when deciding the number of processes to\nlaunch.\n--------------------------------------------------------------------------\n)'
